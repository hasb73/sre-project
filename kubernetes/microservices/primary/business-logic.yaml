apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-logic
  namespace: app
  labels:
    app: business-logic
    tier: backend
    region: primary
spec:
  replicas: 3
  selector:
    matchLabels:
      app: business-logic
  template:
    metadata:
      labels:
        app: business-logic
        tier: backend
        region: primary
    spec:
      imagePullSecrets:
      - name: acr-secret
      containers:
      - name: business-logic
        image: sreproject01.azurecr.io/business-logic:v1.1
        ports:
        - containerPort: 8081
          name: http
          protocol: TCP
        env:
        - name: DATABASE_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: db_host
        - name: DATABASE_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: db_port
        - name: DATABASE_NAME
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: db_name
        - name: DATABASE_USER
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: db_user
        - name: DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgresql-secret
              key: password
        - name: REGION
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: region
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: log_level
        - name: ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: environment
        - name: DATA_INGEST_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: data_ingest_url
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: business-logic
  namespace: app
  labels:
    app: business-logic
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: business-logic
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
